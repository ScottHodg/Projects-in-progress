# Projects-in-progress


# 1. Sports/Hockey Analytics #inprogress
Idea: Comparing Toronto Maple Leafs’ Auston Matthews' shot locations and goal probability under different coaching regimes using an automated ETL pipeline.

MLOps/ETL Component: * ETL: Use the nhl-api to extract play-by-play data. (maybe using Moneypuck instead)

Pipeline: Automate the cleaning and feature engineering (e.g., calculating distance from net) using Apache Airflow (orchestration).

Tracking: Use MLflow (free/open-source) to track model versions that predict "Expected Goals" (xG) based on shot metadata.

# 2. ML/AI Spark project (These projects were generated by AI)
Goal: Demonstrate "Big Data" processing and scalable machine learning using PySpark.

Tip: Use the Databricks Community Edition (Free) to run these, as it provides a managed Spark environment.

Idea 1: Real-Time E-commerce Fraud Detection #todo

Concept: Use the IEEE-CIS Fraud Detection dataset to build a classification model.

Spark Skill: Use pyspark.ml for pipelines and pyspark.sql for handling millions of transaction rows.

MLOps Twist: Implement MLflow inside Databricks to log your Spark model's hyperparameters.

Idea 2: Log Anomaly Detection for Cybersecurity #todo

Concept: Analyze massive server logs (e.g., HDFS log dataset) to detect suspicious patterns or IP blacklisting.

Spark Skill: Use Spark Streaming (structured streaming) to simulate processing data in real-time.

The "Experience" Factor: This mimics how companies use Spark to monitor system health at scale.

Idea 3: Movie/Product Recommendation Engine #todo

Concept: Use the MovieLens dataset to build a collaborative filtering model.

Spark Skill: Use the ALS (Alternating Least Squares) algorithm in Spark MLlib, which is the industry standard for scalable recommendations.

Goal: Process a dataset larger than what your RAM could normally handle using Spark's distributed computing.

# 3. Docker project
Idea: Use Docker for a project to learn the ins and outs. [docker](https://hub.docker.com/search)
The "Slim" Challenge (Image Optimization) #todo

Concept: Build a Docker image for a heavy library like PyTorch or TensorFlow.  

Docker Skill: Use a python:3.9-slim or alpine base image and multi-stage builds to reduce the image size from 2GB to under 500MB.

The Goal: Understanding how to make production-ready, lightweight container
# 4. Reworking/Expanding past projects/assignments #inprogress
reworking student assignments

# 5. Finance/Crypto Real-Time Pipeline #todo
Idea: Building a "Volatility Guard" that predicts price spikes in Bitcoin/Ethereum and triggers alerts.

MLOps/ETL Component: * ETL: Use Python (Pandas/Requests) to pull data from the Binance or Coinbase API every hour.

Data Versioning: Use DVC (Data Version Control) to version your datasets. This mimics how Databricks handles "Delta Lake" versions.

Deployment: Containerize the prediction engine using Docker and deploy it as a microservice (similar to how SageMaker endpoints work).

# 6. Retail/E-commerce Demand Forecasting #todo
Idea: Predicting inventory needs for a small business using public "Brazilian E-Commerce" datasets or simulated Shopify data.

MLOps/ETL Component: * Storage: Mimic a "Lakehouse" by using DuckDB or SQLite to store transformed tables.

CI/CD: Set up GitHub Actions to automatically run your unit tests and retrain the model whenever you push new code (simulating Azure ML Pipelines).

Monitoring: Use Evidently AI (open-source) to monitor "Data Drift" detecting if the new incoming sales data looks significantly different from your training data.
